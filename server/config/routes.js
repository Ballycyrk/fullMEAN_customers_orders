var customer        = require('../controllers/customers.js');
var order           = require('../controllers/orders.js');
var product         = require('../controllers/products.js');

module.exports    = function(app){
  // app.get('/people',        function(req, res){ person.index(req, res) })
  // app.post('/new',          function(req, res){ person.create(req, res) })
  // app.post('/destroy',      function(req, res){ person.destroy(req, res) })
  // app.get('/:name',         function(req, res){ person.show(req, res) })
}
